# Site

Static bilingual catalog. English is default; the header switches to 中文 and stores the choice in `localStorage`.

```bash
cd website
python3 -m http.server 4173
# open http://127.0.0.1:4173
```

GitHub Pages deploys this folder from `.github/workflows/pages.yml`.
